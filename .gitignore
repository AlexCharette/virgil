# Build output (regenerate with `npm run build`)
dist/

# Preview gallery (regenerate with `npm run preview`)
previews/

# OS cruft
.DS_Store
screenshots/

# Impeccable critique archive (local working notes)
.impeccable/

# Stray packaged build at repo root (use dist/ via npm run build)
virgil.zip
